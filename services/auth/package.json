{
  "name": "auth",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch ./src/index.ts",
    "build": "docker build -t anysystem-service/auth --no-cache ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "nodemon": "^3.1.7"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "bun": "^1.1.29",
    "moment": "^2.30.1"
  }
}
